-- CONSULTA SQL QUE CALCULA O VALOR TOTAL DE UM PEDIDO
-- A SER PAGO PELO CLIENTE NA LOJA DBURGER
SELECT  SUM(IP.QT_PEDIDO * IP.VL_UNITARIO) VAL_TOT_PEDIDO
FROM    DB_ITEM_PEDIDO IP
WHERE 	IP.NR_LOJA = &informe_numero_loja
AND		IP.NR_PEDIDO = &informe_numero_pedido;

