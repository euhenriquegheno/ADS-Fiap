SET SERVEROUTPUT ON;
DECLARE
V_QTD_TOT_PRODUTOS_VENDIDOS NUMBER;
V_STATUS_PROCESSAMENTO VARCHAR2(40) := 'NÃO PROCESSADO';
BEGIN
	PRC_DB_CALC_VENDAS_LOJA_PROD_ANO_MES
    (
	P_STATUS_PROCESSAMENTO=>V_STATUS_PROCESSAMENTO,
	P_NR_MES=>1,
    P_QT_TOT_PRD_VENDIDOS=>V_QTD_TOT_PRODUTOS_VENDIDOS,
    P_NR_ANO=>2024,
	P_NR_LOJA=>1
    );
	DBMS_OUTPUT.PUT_LINE('STATUS DO PROCESSAMENTO:' || V_STATUS_PROCESSAMENTO);
END;
