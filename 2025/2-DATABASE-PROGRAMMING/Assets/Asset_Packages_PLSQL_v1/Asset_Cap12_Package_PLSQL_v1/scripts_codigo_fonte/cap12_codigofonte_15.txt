CREATE OR REPLACE PACKAGE PKG_DB_COMERCIAL AS
   -- Variável para conversão de gramas para onças
   v_converte_grama_onca CONSTANT NUMBER := 1 / 28.3495; -- 1 grama equivale a 0.03527396 onças
    -- Variável global para conversão de quilômetros para milhas
   V_CONVERTE_KM_MILHAS CONSTANT NUMBER := 0.621371;
   -- Função para converter gramas para onças
   FUNCTION FUN_DB_GRAMAS_PARA_ONCAS(p_qt_gramas NUMBER) RETURN NUMBER;
   -- Procedure para exibir o resultado da conversão
   PROCEDURE PRC_DB_EXIBIR_CONVERSAO_GRAMA_ONCA(p_gramas NUMBER);

    -- VEJA QUE AGORA TROUXEMOS A FUNCTION PARA DENTRO
    -- DA PACKAGE SPECIFICATION.
    FUNCTION FUN_DB_PEGA_NOME_CLIENTE(
    P_NR_CLIENTE DB_CLIENTE.NR_CLIENTE%TYPE) RETURN VARCHAR2;
    
    -- VEJA QUE AGORA TROUXEMOS A PROCEDURE PARA DENTRO
    -- DA PACKAGE SPECIFICATION.
    PROCEDURE PRC_DB_CALC_VENDAS_LOJA_PROD_ANO_MES (
    P_NR_LOJA IN DB_PRODUTO_LOJA.NR_LOJA%TYPE,
    P_NR_ANO IN NUMBER,   P_NR_MES IN NUMBER,
    P_QT_TOT_PRD_VENDIDOS OUT NUMBER,P_STATUS_PROCESSAMENTO IN OUT VARCHAR2);
END PKG_DB_COMERCIAL;
